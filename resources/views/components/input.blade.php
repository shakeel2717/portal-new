<div>
  <div class="form-group">
    <label for="{{ $name }}" class="form-label">{{ $placeholder }}</label>
    <input type="{{ $type }}" name="{{$name}}" class="form-control" id="{{ $name }}" aria-describedby="{{ $name }}" placeholder="{{ $placeholder }} " {{ $attribute }}>
  </div>
</div>